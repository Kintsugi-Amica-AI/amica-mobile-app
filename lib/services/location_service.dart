class LocationService {
  Future<Map<String, double>?> getCurrentLocation() async {
    // TODO: Request location permissions and return device coordinates.
    return null;
  }

  Stream<Map<String, double>> watchLocation() async* {
    // TODO: Stream live location updates during active SOS alerts.
  }
}
